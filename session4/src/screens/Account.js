import React from 'react';
import { Text, View } from 'react-native';

const Acccount = ({route}) => {
  return (
    <View>
      <Text style={{textAlign:'center', fontSize:20, height:30}}>Account Page</Text>
    </View>
  )
}

export default Acccount